---
title: "FAQ"
heading: "Frequently Asked Questions"
description: "Answers to common questions about our commercial roofing and asphalt paving services across New Jersey."
hasPage: true
itemsHasPage: false
addToMenu:
  - menu: "main-menu"
    parent: "resources"
    order: 2
---
import ContentRenderer from "@/components/ContentRenderer/ContentRenderer.astro";
import { query, sortByOrder } from "@/utils/query";

{/* General FAQs only — service-specific FAQs (those with a `service`
    reference) render on their own service pages, not here. */}
<section class="py-[30px] lg:py-[50px]">
  <div class="container">
    <ContentRenderer
      query={query("faq")
        .where((entry) => !entry.data.service)
        .orderBy(sortByOrder())}
      variant="AccordionVariant"
    />
  </div>
</section>
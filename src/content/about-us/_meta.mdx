---
title: "About"
description: "John's Pro Roofing is a local New Jersey company dedicated to straightforward service, honest communication, and commercial roofing and paving projects completed the right way."
hasPage: true
itemsHasPage: false
heading:
  before: "We do the work, but we"
  text: "stand"
  after: "on trust."
addToMenu:
  - menu: "main-menu"
    parent: "resources"
    order: 1
  - menu: "footer-menu"
redirectFrom: ["/about"]
---
import AboutIntro from "@/components/AboutIntro.astro";
import ContentRenderer from "@/components/ContentRenderer/ContentRenderer.astro";
import CtaBanner from "@/components/CtaBanner.astro";
import { query, sortByOrder } from "@/utils/query";
import { ctaData } from "@/content/siteData";
import differenceBg from "@/assets/commercial-6.webp";

{/* Intro — same block as the homepage, but the primary button is the main
    CTA (not a self-link back to About) with "See Our Work" as the secondary. */}
<section class="pt-[80px] pb-[120px] relative bg-[var(--color-canvas-primary)]">
  <div class="container">
    <AboutIntro
      pretitle="Local. Family Run."
      title="Built on trust."
      accentTitle="Roofed"
      titleAfter="for life."
      description="John's Pro Roofing LLC was founded on a simple belief: every property manager and building owner deserves a contractor they can actually trust. We're a local New Jersey company — not a franchise, not a chain — just a dedicated crew that shows up on time, does the job right, and stands behind every job we complete. From a single roof repair to a full flat-roof system or a parking lot repave, we treat every project with the same care and precision."
      primaryButton={{ text: ctaData.text, href: "/contact-us" }}
      secondaryButton={{ text: "See Our Work", href: "/projects" }}
    />
  </div>
</section>

{/* Stats — driven by the `stats` collection (same StatsVariant as the homepage). */}
<ContentRenderer
  query={query("stats").orderBy(sortByOrder())}
  variant="StatsVariant"
  pretitle="By the Numbers"
  body={[
    "We're a local New Jersey company you can hold accountable — no franchise, no call center, just a crew that answers the phone and stands behind its work.",
  ]}
>
  <h2 slot="heading" class="section-title !text-[clamp(36px,5vw,56px)] leading-[1.05]">
    Built on results,
    <span class="italic text-accent"> backed </span>
    by our warranty.
  </h2>
</ContentRenderer>

{/* The Difference — driven by the `selling-points` collection, rendered as the
    red numbered callout band (same ServiceCalloutVariant as "Who We Serve"). */}
<ContentRenderer
  query={query("selling-points").orderBy(sortByOrder())}
  variant="ServiceCalloutVariant"
  pretitle="Why Choose Us"
  title="The John's Pro Roofing difference"
  description="What sets us apart on every commercial roofing and paving project across New Jersey."
  backgroundImage={differenceBg}
  ctaText={ctaData.text}
  ctaLink="/contact-us"
/>

{/* Mission / Vision / Promise — image cards driven by the `about-us` collection. */}
<ContentRenderer
  query={query("about-us").orderBy(sortByOrder())}
  variant="ImageCardVariant"
  columns={3}
  pretitle="What Drives Us"
  title="Our mission, vision, and promise"
  description="The principles behind every roof we install and every lot we pave."
/>

{/* Close with the main CTA. */}
<CtaBanner />
